count=10

if [ "$1" = "-h" ]; then
	echo "$0 - Display the most used commands"
	echo
	echo "Usage: $0 [count]"
	exit
elif [[ "$1" =~ ^[0-9]+$ ]]; then
	count=$1
fi

history | awk '{print $2}' | awk 'BEGIN {FS="|"} {print $1}' | \
	sort | uniq -c | sort -nr | head -n $count
